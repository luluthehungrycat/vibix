[package]
name = "vibix-kernel"
version = "0.1.0"
edition = "2021"
description = "VIBIX 64-bit kernel (Rust port)"

[features]
default = []
debug = []

[lib]
crate-type = ["staticlib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
